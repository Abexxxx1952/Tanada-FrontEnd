import { NotFound } from "../srcApp/pages/not-found";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tanada - NotFound",
  description: "Travel Blog",
  icons: "/icons/logo.svg",
};

export default NotFound;
