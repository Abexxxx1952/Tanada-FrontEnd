import { TravelsPageWithOutUser } from "@/srcApp/pages/travels";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Travels",
  description: "Travel Blog",
  icons: "/icons/logo.svg",
};

export default TravelsPageWithOutUser;
